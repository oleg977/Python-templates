# Django Basic Template (django_basic)

Базовый, чистый шаблон проекта Django, разработанный для быстрого старта новых веб-приложений с соблюдением лучших практик (Bests Practices).

---

## 🚀 1. Обзор структуры

Проект следует стандартной структуре:

* **`myproject/`**: Основной каталог настроек (`settings.py`, `urls.py`).
* **`myapp/`**: Основное приложение для общих функций (главная страница, базовые представления).
* **`templates/`**: Глобальные шаблоны (`base.html`).

## 2. ⚙️ Установка и запуск

### 2.1. Требования

Для запуска требуется **Python 3.10+** и **виртуальное окружение**.

### 2.2. Настройка

1.  **Активация окружения и установка зависимостей:**
    ```bash
    python -m venv .venv
    source .venv/bin/activate  # или .\.venv\Scripts\activate в Windows
    pip install -r requirements.txt
    ```

2.  **Переменные окружения:**
    Создайте файл **`.env`** в корне `django_basic/` на основе `.env.example`.

3.  **Миграции:**
    ```bash
    python manage.py makemigrations myapp
    python manage.py migrate
    ```

### 2.3. Запуск сервера

Проверьте, что приложение `myapp` корректно подключено:

```bash
python manage.py runserver