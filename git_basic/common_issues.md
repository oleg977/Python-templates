# Решение распространённых проблем

## Ошибка `fatal: pathspec`
Причина: Файл не найден.
Решение: Убедитесь, что путь указан правильно:
```bash
git add new_shoplist/manage.py

В репозитории есть изменение а у меня нет
git pull origin main
git push origin main

Конфликты при слиянии, добавить изменение
git add <file_name>
git commit -m "Resolve merge conflict"